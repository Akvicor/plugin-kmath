name: CD

on:
  release:
    types:
      - published

jobs:
  cd:
    uses: halo-sigs/reusable-workflows/.github/workflows/plugin-cd.yaml@v3
    permissions:
      contents: write
    with:
      java-version: 21
      node-version: 20
      pnpm-version: 10
      skip-appstore-release: true
      ui-path: "console"
